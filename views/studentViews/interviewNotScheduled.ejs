<div class="d-flex justify-content-center align-items-center" style="min-height: 80vh; background-color: #f8f9fa;">
  <div class="card shadow-sm text-center p-5" style="max-width: 600px; border: none; border-radius: 1rem;">
    
    <div class="mb-3">
      <i class="bi bi-calendar-x text-danger" style="font-size: 4rem;"></i>
    </div>

    <h3 class="mb-3">Interview Not Yet Scheduled</h3>
    <p class="text-muted mb-4">
      The interview for <strong><%= application.postingId.title %></strong> at 
      <strong><%= application.postingId.userId.name %></strong> has not been scheduled yet.
    </p>

    <div class="alert alert-secondary text-start mx-auto" style="max-width: 400px;">
      <i class="bi bi-clock me-2"></i>
      Please check back later. You’ll be notified once your interview has been arranged.
    </div>

    <div class="mt-4">
      <a href="/api/student/<%= student._id %>/applications/<%= application._id %>" 
         class="btn btn-outline-primary px-4 py-2">
        <i class="bi bi-arrow-left"></i> Back to Application
      </a>
    </div>

  </div>
</div>
