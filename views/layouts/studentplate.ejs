<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Student Page</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.6/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-4Q6Gf2aSP4eDXB8Miphtr37CMZZQ5oXLH2yaXMJ2w8e2ZtHTl7GptT4jmndRuHDT" crossorigin="anonymous">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">

</head>
<body class="d-flex flex-column vh-100">
    <nav class="navbar sticky-top navbar-expand-lg navbar-dark bg-dark">
  <div class="container-fluid">
    <a class="navbar-brand" href="#">CampusLink</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav">
        <li class="nav-item">
          <a class="nav-link" aria-current="page" href="/api/student/<%=student._id%>">Profile</a>
        </li>

        <li class="nav-item">
          <a class="nav-link <%= hasAcceptedInterview ? 'disabled text-secondary' : '' %>"
          href="<%= hasAcceptedInterview ? '#' : `/api/student/${student._id}/opportunities` %>"
          aria-disabled="<%= hasAcceptedInterview ? 'true' : 'false' %>"
          data-bs-toggle="tooltip"
          title="<%= hasAcceptedInterview ? 'You already have an accepted offer' : '' %>">Opportunities
          </a>
        </li>

        <li class="nav-item">
          <a class="nav-link" href="/api/student/<%=student._id%>/applications">Applications</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="/api/student/<%=student._id%>/interviews">Interviews</a>
        </li>
        <!-- <li class="nav-item">
          <a class="nav-link" href="/campgrounds/new">New Campground</a>
        </li>
        <li class="nav-item">
          <a class="nav-link disabled" aria-disabled="true">Disabled</a>
        </li> -->
      </ul>
      <ul class="navbar-nav ms-auto"> <!-- pushes items to the far right -->
        <li class="nav-item">
          <a href="/login" class="btn btn-outline-danger">Log Out</a>
        </li>
      </ul>
    </div>
  </div>
</nav>

<%
  const acceptedInterviews = (interviews || []).filter(i => i && i.result === 'Accepted' && i.applicationId && i.applicationId.postingId);
  if (acceptedInterviews.length > 0) {
%>
  <div class="bg-success bg-opacity-10 border-bottom border-success shadow-sm py-2">
    <div class="container">
      <marquee behavior="scroll" direction="left" scrollamount="5" class="fw-semibold text-success">
         Congratulations! You’ve been accepted for 
        <% acceptedInterviews.forEach((i, index) => { %>
          <%= i.applicationId.postingId.title %> at <%= i.applicationId.postingId.userId.name %>
          <%= index < acceptedInterviews.length - 1 ? ' • ' : '' %>
        <% }); %>
      </marquee>
      <div class="text-center text-secondary fw-semibold mt-1">
         Note that you are <strong>NOT allowed to apply for other opportunities.</strong>
      </div>
    </div>
  </div>
<% } %>






    <div class="container mt-5">
    <%-body%>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.8/dist/umd/popper.min.js" integrity="sha384-I7E8VVD/ismYTF4hNIPjVp/Zjvgyol6VFvRkX/vR+Vc4jQkC+hVqc2pM8ODewa9r" crossorigin="anonymous"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.6/dist/js/bootstrap.min.js" integrity="sha384-RuyvpeZCxMJCqVUGFI0Do1mQrods/hhxYlcVfGPOfQtPJh0JCw12tUAZ/Mv10S7D" crossorigin="anonymous"></script>
    <%-include('../partials/footer')%>
</body>
</html>